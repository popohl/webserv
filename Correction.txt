Test fait avec la config de PIERRE

curl -X POST -H "Content-Type: plain/text" --data "BODY IS HERE write something shorter or longer than
body limit" http://localhost:9000

Fait un bad request alors que POST est Allowed


Ou vont les requetes post ?
Il faut rajouter un bouton delete (J'en ai pas trouve de fonctionnel sur le net, on pourra faire un Telnet ou un Postman)

- upload some ﬁle to the server and get it back
- setup a default ﬁle to search for if you ask for a directory  (est ce qu'on parle de l'autoindex)
- setup a list of method accepted for a certain route (ex: try to delete something with and without permission) ==> comment on fait ca ?


Probleme si on se connecte sur http://localhost:9000/img   -> pending infini :(  (souci avec l'autoindex ?)

Pour que l'autoindex fonctionne on doit utiliser le root de la location rules (mais je ne suis pas arrive a le trouver ^^)


